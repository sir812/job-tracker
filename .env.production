# Production environment — used by `vite build` and Docker image builds.
# These are PUBLIC values (no secrets). Safe to commit.
VITE_API_BASE_URL=https://job-tracker-production-a2e6.up.railway.app/api
VITE_AI_API_BASE_URL=https://job-tracker-production-a2e6.up.railway.app/api
